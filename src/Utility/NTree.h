#pragma once

#include "TreeNode.hpp"
#include "../Mob/MobModel.h"
/*
class NTree
{
   friend class boost::serialization::access;
    template<class Archive>
    void save(Archive & ar, const unsigned int version) const
    {


    }

    template<class Archive>
    void load(Archive & ar, const unsigned int version)
    {



    }

    BOOST_SERIALIZATION_SPLIT_MEMBER()
public:
    NTree();
    bool addNode(MobModel* value, string parentName = "none");
    bool removeNode(MobModel* value);
    MobModel* getRootData();
    MobModel* getDataByNodeName(std::string name);
    const TreeNode<MobModel>* findNodeByName(std::string name);

private:
    TreeNode<MobModel> rootNode;
};

*/
